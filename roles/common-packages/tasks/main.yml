---
- name: install packages for all systems
  apt: package={{ item }} state=present
  with_items:
    - vim
    - bc
    - lsof
  become: true
  tags:
    - vim
    - bc
    - lsof
    - common-packages

