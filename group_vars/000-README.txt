
# group variables for these playbooks

remote_user = which user to ssh into the remote system as

   => the key defined in ~/.ssh/config for the remote host
      must work vs. this remote_user, and the remote_user
      should have sudo access on the remote host

